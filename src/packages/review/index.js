const videoBaseUrl = "https://videoapi-vod.shopee.com.my/api/vod/preupload";
const { baseUrl } = require("../../config");

const uploadImage = () => {
  const url = `${baseUrl.rating}/image_upload`;
};
